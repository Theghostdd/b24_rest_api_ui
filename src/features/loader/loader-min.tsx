import React from "react";
import './loader.css';

const LoaderMin: React.FC = () => (
    <section className="loader-overlay-min">
        <div className="loader-min">
            <div></div>
        </div>
    </section>
);

export default LoaderMin;
